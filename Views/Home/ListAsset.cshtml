<div>
    <a href="#/asset/create" class="btn">Create</a>
</div>
<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <tr>
            <th>Asset Id</th>
            <th>Asset Name</th>
            <th>Room Name</th>
            <th>Building Name</th>
            <th>Site Name</th>
            <th>Country</th>
            <th>State</th>
            <th>Is Active</th>
            <th></th>
            <th></th>
        </tr>
        <tr ng-repeat="item in rooms">
            <td>{{ item.AssetId }}</td>
            <td>{{ item.AssetName }}</td>
            <td>{{ item.RoomName }}</td>
            <td>{{ item.BuildingName }}</td>
            <td>{{ item.SiteName }}</td>
            <td>{{ item.Country }}</td>
            <td>{{ item.State }}</td>
            <td>{{ item.DateofBirth | date }}</td>
            <td>
                <span class="label" ng-class="{true:'label-success', false:'label-danger', '':'hidden'}[item.IsActive]">
                    {{ item.IsActive ? 'Active' : 'In Active' }}
                </span>
            </td>
            <td>
                <a href="#/asset/edit/{{item.RoomId}}" class="glyphicon glyphicon-edit"></a>
            </td>
            <td>
                <a href="#/asset/delete/{{item.RoomId}}" class="glyphicon glyphicon-trash"></a>
            </td>
        </tr>
    </table>
</div>