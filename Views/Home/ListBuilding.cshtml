<div>
    <a href="#/building/create" class="btn">Create</a>
</div>
<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <tr>
            <th>Building Id</th>
            <th>Building Name</th>
            <th>Site Name</th>
            <th>Country</th>
            <th>State</th>
            <th>Is Active</th>
            <th></th>
            <th></th>
        </tr>
        <tr ng-repeat="item in buildings">
            <td>{{ item.BuildingId }}</td>
            <td>{{ item.BuildingName }}</td>
            <td>{{ item.SiteName }}</td>
            <td>{{ item.Country }}</td>
            <td>{{ item.State }}</td>
            <td>
                <span class="label" ng-class="{true:'label-success', false:'label-danger', '':'hidden'}[item.IsActive]">
                    {{ item.IsActive ? 'Active' : 'In Active' }}
                </span>
            </td>
            <td>
                <a href="#/building/edit/{{item.BuildingId}}" class="glyphicon glyphicon-edit"></a>
            </td>
            <td>
                <a href="#/building/delete/{{item.BuildingId}}" class="glyphicon glyphicon-trash"></a>
            </td>
        </tr>
    </table>
</div>